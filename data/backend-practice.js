
/* XMLHttpRequest = Old way to make HTTP requests in JavaScript (before fetch).

open('GET', url) = Prepares a request to fetch data from a given URL using the GET method.

send() = Actually sends the request. */

const xhr =  new XMLHttpRequest();

xhr.open('GET');

